# Data

The original operational dataset is not included because it contains
confidential user conversations.

`sample_messages.csv` contains synthetic examples created exclusively
to demonstrate the expected data structure. These examples do not
represent real users or conversations.

Required columns:

- `text`: Thai or English message
- `label`: 0 for negative and 1 for potential policy violation
